{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "mobile.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/metadata",
      "handler": "mobile.tables",
      "config": {
        "policies": []
      }
    }
  ]
}
